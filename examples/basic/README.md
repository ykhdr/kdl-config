# Basic KDL Config Example

This example demonstrates how to load and validate a simple KDL configuration file using the `kdlconfig` library.

## Files

- `main.go` — example Go program.
- `config.kdl` — sample KDL config.
