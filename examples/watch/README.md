# Watcher KDL Config Example

This example shows how to use `kdlconfig.Watch` for hot‑reloading your config on file changes.

## Files

- `main.go` — example Go program that watches `config.kdl`.
- `config.kdl` — sample KDL config.
